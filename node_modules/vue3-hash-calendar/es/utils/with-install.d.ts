import type { App, Component } from 'vue';
declare type EventShim = {
    new (...args: any[]): {
        $props: {
            onClick?: (...args: any[]) => void;
        };
    };
};
export declare type WithInstall<T> = T & {
    install(app: App): void;
} & EventShim;
export declare const withInstall: <T extends Component<any, any, any, import("vue").ComputedOptions, import("vue").MethodOptions>>(options: T) => WithInstall<T>;
export {};
